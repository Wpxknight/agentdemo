# 应用配置文件路径（models / mcpServers / clusters）
AIOP_CONFIG=./config.jsonc

# 日志级别: trace|debug|info|warn|error
LOG_LEVEL=info

# ── E2B 沙箱（S2 起用；config.sandbox.enabled=true 时生效）──
E2B_API_KEY=

# ── 认证（S7）：会话 JWT 签名密钥（HS256），生产务必设置强随机值 ──
AIOP_JWT_SECRET=

# ── MySQL（S5 起用；连接走环境变量，密码 base64）──
MYSQL_HOST=mysql.db.internal
MYSQL_PORT=3306
MYSQL_DATABASE=ai_ops
MYSQL_USER=ai_ops
# echo -n 'your-password' | base64
MYSQL_PASSWORD_BASE64=
MYSQL_SSL=false
MYSQL_POOL_SIZE=10
