export type { Model, Models } from '@earendil-works/pi-ai';

/** Pi model calls are delegated directly to Models; this layer intentionally owns no retry policy. */
export const PI_KERNEL_VERSION = '0.82.1';
