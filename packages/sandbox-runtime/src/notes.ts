/**
 * 沙箱启用时注入系统提示的静态规范片段（与按用户动态生成的 userhome.ts 提示并列注入）。
 */

/** 服务类代码的启动与访问链接规范：让用户能直接点开沙箱里跑起来的服务。 */
export const SANDBOX_SERVICE_NOTE = [
  '沙箱内启动服务的规范（开发/修改 Web 服务、API、前端应用等需要运行验证时）：',
  '1. 监听地址必须是 0.0.0.0（全部 IP），不要只绑 127.0.0.1，否则沙箱外无法访问。',
  '2. 用 nohup + & 后台启动并重定向日志（如 `nohup npm run dev -- --host 0.0.0.0 >/tmp/app.log 2>&1 &`），前台启动会一直占住命令不返回。启动后先在沙箱内 curl 探活确认端口就绪，失败则查日志。',
  '3. 就绪后执行 `hostname -i` 获取沙箱 IP，在最终回复中把访问链接以 Markdown 链接原样给出：`http://<沙箱IP>:<端口>`，并注明服务名/用途。',
  '4. 提醒用户：链接在沙箱存活期间有效，沙箱空闲回收后失效。',
].join('\n');
