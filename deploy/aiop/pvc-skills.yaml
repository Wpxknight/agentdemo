apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aiop-skills
  namespace: aios-system
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 5Gi
