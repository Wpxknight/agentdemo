apiVersion: v1
kind: Service
metadata:
  name: aiop-server
  namespace: aiop
  labels: { app: aiop, component: server }
spec:
  selector: { app: aiop, component: server }
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
